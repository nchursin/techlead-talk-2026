#import "../theme.typ": *

#objection(
  [Где это всё может пригодиться?],
  [Что в нашей жизни за последнее время появилось такое, где всё это важно?],
)

#slide(
  [Кодинг в 2к26],
  [
    #card([
      Было такое:
      - LLM что-то уверенно нагенерил
      - на первый взгляд даже похоже на рабочее
      - первый же нормальный тест всё развалил
    ])
  ],
)

#slide(
  [TDD и LLM: антипаттерн],
  [
    #card([
      #text(size: 20pt)[
        - Просишь написать тест на готовый код
        - Тавтологичный тест: LLM знает, что написано
        - Тест просто подтверждает реализацию, не поведение
      ]
    ])
    #v(0.14in)
    #card(
      [
        #text(size: 18pt)[Это не тестирование — это комментарий в виде теста]
      ],
      fill-color: panel-soft,
    )
  ],
  kicker: [TDD и LLM],
)

#slide(
  [Тест как спека для LLM],
  [
    #card([
      #text(size: 20pt)[
        - Ты пишешь тест с ожидаемым поведением
        - LLM получает только тест — реализации ещё не существует
        - Тест описывает ЧТО, LLM решает КАК
        - Ревью после каждого зелёного теста
      ]
    ])
  ],
  kicker: [TDD и LLM],
)

#slide(
  [YOLO-режим: инструмент выбора],
  [
    #card([
      #text(size: 20pt)[
        - Иногда нужна скорость — «просто напиши без тестов»
        - Это ок, если *осознанно*: ты берёшь технический долг
        - Явно назови риски — не замалчивай их
      ]
    ])
  ],
  kicker: [TDD и LLM],
)
